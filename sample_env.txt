# Copy this file to .env and fill in credentials as needed
# cp sample_env.txt .env

# ---------------------------
# Pipeline storage backend
# ---------------------------
# LOCAL: use local filesystem (default)
# BLOB: Azure Blob Storage (read/write)
# SNOWFLAKE: Snowflake internal stage (read/write)
export DATA_PIPELINE_DB=LOCAL

# ---------------------------
# Output locations
# ---------------------------
# Storms registry
export RESULTS_DIR=results
export STORMS_FILE=storms.json

# Base and impact views
export ROOT_DATA_DIR=geodb
export VIEWS_DIR=aos_views

# Report files (optional - defaults shown)
# JSON reports subdirectory under RESULTS_DIR (default: jsons)
# export REPORTS_JSON_DIR=jsons
# HTML template path (default: impact-report-template.html in project root)
# export REPORT_TEMPLATE_PATH=impact-report-template.html

# ---------------------------
# Snowflake (required for data retrieval)
# ---------------------------
export SNOWFLAKE_ACCOUNT=your_account
export SNOWFLAKE_USER=your_user
export SNOWFLAKE_PASSWORD=your_password
export SNOWFLAKE_WAREHOUSE=your_warehouse
export SNOWFLAKE_DATABASE=your_database
export SNOWFLAKE_SCHEMA=your_schema

# ---------------------------
# Snowflake Stage (only if DATA_PIPELINE_DB=SNOWFLAKE)
# ---------------------------
# Name of the Snowflake stage to use for file storage
# The stage must exist in your Snowflake database/schema
# Example: CREATE STAGE my_stage;
# export SNOWFLAKE_STAGE_NAME=my_stage

# ---------------------------
# Azure Blob Storage (only if DATA_PIPELINE_DB=BLOB)
# ---------------------------
# ACCOUNT_URL example: https://<account_name>.blob.core.windows.net
# SAS_TOKEN should start with ?sv=...
# export ACCOUNT_URL=
# export SAS_TOKEN=

# ---------------------------
# Hazard source data read mode (optional)
# ---------------------------
# Where to read UPSTREAM hazard source data from (hurricane wind/gust
# envelopes, tracks, precip/runoff) -- independent of DATA_PIPELINE_DB above,
# which only controls where THIS repo's own output/cache is written.
# SNOWFLAKE (default): query TC_ENVELOPES_COMBINED/TC_GUST_ENVELOPES_COMBINED/
#   TC_TRACKS/MET_FORECASTS directly, same as always -- no extra vars needed.
# LOCAL: read directly from TC-ECMWF-Forecast-Pipeline's own local-disk
#   output, mainly for local end-to-end testing without a live Snowflake
#   connection populated with real storm data.
# BLOB: same data, from an Azure Blob container.
# export HAZARD_DATA_SOURCE=SNOWFLAKE

# Required only if HAZARD_DATA_SOURCE=LOCAL. Point these at
# TC-ECMWF-Forecast-Pipeline's own output directories (WIND_EXTRACTED_DIR,
# TRANSFORMED_DATA_DIR, MET_DATA_DIR in that repo).
# export HAZARD_LOCAL_WIND_DIR=/path/to/TC-ECMWF-Forecast-Pipeline/wind_extracted
# export HAZARD_LOCAL_TRACKS_DIR=/path/to/TC-ECMWF-Forecast-Pipeline/tc_data_transformed
# export HAZARD_LOCAL_MET_DIR=/path/to/TC-ECMWF-Forecast-Pipeline/met_data

# Required only if HAZARD_DATA_SOURCE=BLOB. Deliberately separate from this
# app's own ACCOUNT_URL/SAS_TOKEN above -- the upstream repo's hazard data
# typically lives in a different storage account/container entirely.
# export HAZARD_BLOB_ACCOUNT_URL=
# export HAZARD_BLOB_SAS_TOKEN=
# export HAZARD_BLOB_CONTAINER=

# ---------------------------
# API tokens (for data fetching)
# ---------------------------
# REQUIRED for school locations via GIGA API
export GIGA_SCHOOL_LOCATION_API_KEY=

# REQUIRED for HealthSites facility fetching
export HEALTHSITES_API_KEY=

# Optional: GeoRepo API credentials (for admin boundaries)
# export GEOREPO_API_KEY=
# export GEOREPO_USER_EMAIL=

# ---------------------------
# GeoSight (optional)
# ---------------------------
# Used by scripts under geosight/ for related-table uploads
# GEOSIGHT_API_KEY should contain the full Authorization header value,
# for example: "Token <token>"
# GEOSIGHT_USER_EMAIL should match the owner email for the API key
# export GEOSIGHT_BASE_URL=https://geosight.unicef.org
# export GEOSIGHT_API_KEY=
# export GEOSIGHT_USER_EMAIL=
